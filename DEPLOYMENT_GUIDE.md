# 📁 文件夹结构说明

在部署到Netlify之前，请按照以下结构组织您的文件：

## 正确的文件夹结构

```
your-project/
├── index.html
├── netlify.toml
├── package.json
├── README.md
├── .gitignore
├── .env.example
└── netlify/
    └── functions/
        ├── chat.js
        └── translate.js
```

## 📝 重要步骤

### 1. 创建文件夹
在您的项目根目录下创建以下文件夹：
```
netlify/functions/
```

### 2. 放置文件
- 将 **chat.js** 文件放入 `netlify/functions/` 文件夹
- 将 **translate.js** 文件放入 `netlify/functions/` 文件夹
- 其他文件（index.html, netlify.toml等）放在项目根目录

### 3. 完整文件清单
确保您有以下文件：

**根目录文件：**
- ✅ index.html
- ✅ netlify.toml
- ✅ package.json
- ✅ README.md
- ✅ .gitignore
- ✅ .env.example

**netlify/functions/ 文件夹中：**
- ✅ chat.js
- ✅ translate.js

## 🚀 部署方法

### 方法1：通过Git仓库（推荐）

1. 在GitHub/GitLab创建新仓库
2. 按照上述结构上传所有文件
3. 登录Netlify
4. 选择 "Import an existing project"
5. 连接您的Git仓库
6. 在环境变量中添加 `DEEPSEEK_API_KEY`
7. 部署

### 方法2：拖拽部署

1. 将所有文件按照上述结构组织在本地文件夹
2. 登录Netlify
3. 直接将整个文件夹拖拽到Netlify部署区域
4. 部署后，在站点设置中添加环境变量 `DEEPSEEK_API_KEY`

### 方法3：Netlify CLI

```bash
# 1. 安装Netlify CLI
npm install -g netlify-cli

# 2. 登录
netlify login

# 3. 在项目目录初始化
netlify init

# 4. 设置环境变量
netlify env:set DEEPSEEK_API_KEY "your_api_key_here"

# 5. 部署
netlify deploy --prod
```

## ⚠️ 常见问题

**问题1：函数无法找到**
- 确保 netlify.toml 文件中的 functions 路径正确：`functions = "netlify/functions"`
- 确保 chat.js 和 translate.js 在正确的文件夹中

**问题2：API调用失败**
- 检查环境变量 DEEPSEEK_API_KEY 是否正确设置
- 在Netlify控制台查看函数日志

**问题3：CORS错误**
- 函数已包含CORS头部，应该不会有问题
- 如果还有问题，检查浏览器控制台的具体错误信息

## 🔍 验证部署

部署成功后，您可以通过以下方式验证：

1. 访问您的Netlify站点URL
2. 打开浏览器开发者工具（F12）
3. 尝试使用AI助手功能
4. 查看Network标签，确认请求发送到 `/.netlify/functions/chat`
5. 在Netlify控制台的Functions标签查看函数调用日志

## 📞 需要帮助？

如果遇到问题，请检查：
- Netlify部署日志
- 浏览器控制台错误
- Netlify Functions日志

---

按照此说明操作，您的应用就能成功部署到Netlify了！
